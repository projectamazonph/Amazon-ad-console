/**
 * Core engine — re-exports from focused modules.
 *
 * This file is now a barrel. All logic lives in engine/*.ts.
 * Kept for backward compatibility with existing imports.
 */
export * from './engine/index';
